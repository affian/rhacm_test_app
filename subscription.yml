---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: mongodb-app
  namespace: mongodb
  labels:
    app: mongodb-app
  annotations:
    apps.open-cluster-management.io/github-path: https://github.com/affian/rhacm_test_app.git
spec:
  channel: mongodb/mongodb-app
  placement:
    placementRef:
      kind: PlacementRule
      name: dev-cluster
